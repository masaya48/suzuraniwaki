---
import Layout from '../src/layouts/Layout.astro';
import Header from '../src/components/Header.astro';
import DRTAbout from '../src/components/DRTAbout.astro';
import MaternityMenu from '../src/components/MaternityMenu.astro';
import Footer from '../src/components/Footer.astro';
---

<Layout title="料金表 | からだサポートSUZURAN">
	<Header />
	<main>
		<section class="page-header">
			<div class="container">
				<h1 class="page-title">料金表</h1>
				<p class="page-description">
					施術メニューの料金・回数券のご案内
				</p>
			</div>
		</section>
		<MaternityMenu />
	</main>
	<Footer />
</Layout>

<style>
	.page-header {
		background: linear-gradient(135deg, #eff6ff 0%, #dbeafe 100%);
		padding: 4rem 0 3rem;
		text-align: center;
	}

	.container {
		max-width: 1200px;
		margin: 0 auto;
		padding: 0 20px;
	}

	.page-title {
		font-size: 2.5rem;
		font-weight: 700;
		color: var(--color-text);
		margin: 0 0 1rem 0;
	}

	.page-description {
		font-size: 1.1rem;
		color: var(--color-text-light);
		margin: 0;
	}

	@media (max-width: 768px) {
		.page-header {
			padding: 3rem 0 2rem;
		}

		.page-title {
			font-size: 2rem;
		}

		.page-description {
			font-size: 1rem;
		}
	}
</style>
