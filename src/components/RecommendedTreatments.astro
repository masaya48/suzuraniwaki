---
import { Image } from "astro:assets";
import imgKokansetsutuu from '../assets/kokansetsutuu.png';
import imgYoutuu from '../assets/youtuu.png';

---

<section class="recommended">
	<div class="container">
		<h2 class="section-title">おすすめ施術</h2>
		<div class="treatments-grid">
			<a href="/menu/kokansetsu" class="treatment-banner">
				<div class="banner-image">
					<Image
						src={imgKokansetsutuu}
						alt="変形性股関節症"
					/>
				</div>
				<div class="banner-content">
					<h3 class="banner-title">変形性股関節症</h3>
					<p class="banner-description">
						手術を勧められた方、歩行に不安を感じている方へ。専門の施術で痛みを軽減し、歩行能力を改善します。
					</p>
					<span class="banner-link">詳しく見る →</span>
				</div>
			</a>
			<a href="/menu/sekichuukan" class="treatment-banner">
				<div class="banner-image">
					<Image
						src={imgYoutuu}
						alt="脊柱管狭窄症"
					/>
				</div>
				<div class="banner-content">
					<h3 class="banner-title">脊柱管狭窄症</h3>
					<p class="banner-description">
						間欠性跛行でお困りの方、手術を避けたい方へ。根本原因にアプローチし、症状の改善を目指します。
					</p>
					<span class="banner-link">詳しく見る →</span>
				</div>
			</a>
		</div>
	</div>
</section>

<style>
	.recommended {
		padding: 4rem 0;
		background: white;
	}

	.container {
		max-width: 1200px;
		margin: 0 auto;
		padding: 0 20px;
	}

	.section-title {
		text-align: center;
		font-size: 2rem;
		font-weight: 700;
		color: var(--color-text);
		margin: 0 0 3rem 0;
		position: relative;
		padding-bottom: 1rem;
	}

	.section-title::after {
		content: '';
		position: absolute;
		bottom: 0;
		left: 50%;
		transform: translateX(-50%);
		width: 60px;
		height: 4px;
		background: var(--color-primary);
		border-radius: 2px;
	}

	.treatments-grid {
		display: grid;
		grid-template-columns: repeat(2, 1fr);
		gap: 2rem;
	}

	.treatment-banner {
		background: white;
		border: 2px solid var(--color-border);
		border-radius: 24px;
		overflow: hidden;
		transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
		box-shadow: 0 4px 20px rgba(0, 0, 0, 0.08);
		display: block;
		text-decoration: none;
	}

	.treatment-banner:hover {
		transform: translateY(-8px);
		box-shadow: 0 16px 48px rgba(212, 129, 111, 0.25);
		border-color: var(--color-primary);
	}

	.banner-image {
		width: 100%;
		aspect-ratio: 2/1;
		overflow: hidden;
	}

	.image-placeholder {
		width: 100%;
		height: 100%;
		background: linear-gradient(135deg, var(--color-cream) 0%, var(--color-cream-light) 100%);
		display: flex;
		align-items: center;
		justify-content: center;
		color: var(--color-primary);
		font-weight: 700;
		font-size: 1.25rem;
		transition: all 0.3s;
	}

	.treatment-banner:hover .image-placeholder {
		transform: scale(1.05);
	}

	.banner-content {
		padding: 2rem;
	}

	.banner-title {
		font-size: 1.75rem;
		font-weight: 700;
		color: var(--color-text);
		margin: 0 0 1rem 0;
		font-family: var(--font-display);
	}

	.banner-description {
		color: var(--color-text-light);
		line-height: 1.8;
		margin: 0 0 1rem 0;
		font-size: 1.05rem;
	}

	.banner-link {
		display: inline-block;
		color: var(--color-primary);
		font-weight: 700;
		transition: all 0.3s;
	}

	.treatment-banner:hover .banner-link {
		transform: translateX(4px);
	}

	@media (max-width: 768px) {
		.recommended {
			padding: 3rem 0;
		}

		.section-title {
			font-size: 1.75rem;
		}

		.treatments-grid {
			grid-template-columns: 1fr;
			gap: 1.5rem;
		}
	}
</style>
