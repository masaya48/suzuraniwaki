---
import Icon from './Icon.astro';
---

<section class="concerns">
	<div class="container">
		<h2 class="section-title">このようなお悩みもお任せ下さい</h2>
		<ul class="concerns-list">
			<li>✓ 頭や首・背中の痛みが良くならない</li>
			<li>✓ 10年以上続く腰痛や肩こりがつらい</li>
			<li>✓ 膝や股関節など下肢の痛み・しびれがつらい</li>
			<li>✓ 坐骨神経痛で足の痺れが３ヶ月以上続いている</li>
			<li>✓ 姿勢の悪さが気になる</li>
			<li>✓ １日も早く健康になりたい</li>
		</ul>
		<div class="symptoms-grid">
			<a href="/menu" class="symptom-button">
				<Icon name="activity" size={36} class="symptom-icon" />
				<span class="symptom-text">頭痛</span>
			</a>
			<a href="/menu" class="symptom-button">
				<Icon name="zap" size={36} class="symptom-icon" />
				<span class="symptom-text">腰痛</span>
			</a>
			<a href="/menu" class="symptom-button">
				<Icon name="alert-circle" size={36} class="symptom-icon" />
				<span class="symptom-text">肩こり</span>
			</a>
			<a href="/menu" class="symptom-button">
				<Icon name="move" size={36} class="symptom-icon" />
				<span class="symptom-text">股関節痛</span>
			</a>
			<a href="/menu" class="symptom-button">
				<Icon name="zap" size={36} class="symptom-icon" />
				<span class="symptom-text">坐骨神経痛</span>
			</a>
			<a href="/menu" class="symptom-button">
				<Icon name="user" size={36} class="symptom-icon" />
				<span class="symptom-text">姿勢矯正</span>
			</a>
		</div>
		<div class="more-menu">
			<a href="/menu" class="btn-more-menu">その他のお悩み</a>
		</div>
	</div>
</section>

<style>
	.concerns {
		padding: 5rem 0;
		background: linear-gradient(
			180deg,
			var(--color-cream-light) 0%,
			var(--color-bg) 100%
		);
		position: relative;
		overflow: hidden;
	}

	.concerns::before {
		content: '';
		position: absolute;
		top: 10%;
		left: -5%;
		width: 30%;
		height: 50%;
		background: radial-gradient(
			circle at center,
			var(--color-accent) 0%,
			transparent 70%
		);
		opacity: 0.08;
		border-radius: 50%;
	}

	.container {
		max-width: 1200px;
		margin: 0 auto;
		padding: 0 40px;
		position: relative;
		z-index: 1;
	}

	.section-title {
		text-align: center;
		font-size: 2.5rem;
		font-weight: 700;
		font-family: var(--font-display);
		color: var(--color-text);
		margin: 0 0 3rem 0;
		position: relative;
		padding-bottom: 1.5rem;
	}

	.section-title::after {
		content: '';
		position: absolute;
		bottom: 0;
		left: 50%;
		transform: translateX(-50%);
		width: 80px;
		height: 5px;
		background: linear-gradient(
			90deg,
			var(--color-primary) 0%,
			var(--color-accent) 100%
		);
		border-radius: 10px;
	}

	.concerns-list {
		list-style: none;
		padding: 0;
		margin: 0 0 3rem 0;
		display: grid;
		grid-template-columns: repeat(2, 1fr);
		gap: 1rem;
		max-width: 800px;
		margin-left: auto;
		margin-right: auto;
	}

	.concerns-list li {
		font-size: 1.05rem;
		color: var(--color-text);
		padding: 1.5rem 1.75rem;
		background: white;
		border-radius: 20px;
		font-weight: 500;
		box-shadow: 0 4px 16px rgba(212, 129, 111, 0.08);
		border: 1px solid var(--color-border);
		transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
	}

	.concerns-list li:hover {
		transform: translateY(-3px);
		box-shadow: 0 8px 24px rgba(212, 129, 111, 0.15);
		border-color: var(--color-primary);
	}

	.symptoms-grid {
		display: grid;
		grid-template-columns: repeat(6, 1fr);
		gap: 1.25rem;
		max-width: 900px;
		margin: 0 auto;
	}

	.symptom-button {
		display: flex;
		flex-direction: column;
		align-items: center;
		gap: 0.875rem;
		padding: 1.75rem 1rem;
		background: white;
		border: 2px solid var(--color-border);
		border-radius: 24px;
		transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
		cursor: pointer;
		box-shadow: 0 4px 16px rgba(212, 129, 111, 0.08);
		position: relative;
		overflow: hidden;
	}

	.symptom-button::before {
		content: '';
		position: absolute;
		top: 0;
		left: 0;
		right: 0;
		bottom: 0;
		background: linear-gradient(
			135deg,
			var(--color-cream-light) 0%,
			var(--color-cream) 100%
		);
		opacity: 0;
		transition: opacity 0.4s;
	}

	.symptom-button:hover::before {
		opacity: 1;
	}

	.symptom-button:hover {
		border-color: var(--color-primary);
		transform: translateY(-6px);
		box-shadow: 0 12px 32px rgba(212, 129, 111, 0.2);
	}

	.symptom-icon {
		position: relative;
		z-index: 1;
		transition: transform 0.4s;
		color: var(--color-primary);
	}

	.symptom-button:hover .symptom-icon {
		transform: scale(1.15);
	}

	.symptom-text {
		font-weight: 700;
		font-family: var(--font-heading);
		color: var(--color-text);
		text-align: center;
		position: relative;
		z-index: 1;
	}

	.more-menu {
		text-align: center;
		margin-top: 2.5rem;
	}

	.btn-more-menu {
		display: inline-block;
		padding: 1rem 2.5rem;
		background: linear-gradient(135deg, var(--color-primary) 0%, var(--color-accent) 100%);
		color: white;
		border-radius: 50px;
		font-weight: 700;
		font-size: 1.05rem;
		transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
		box-shadow: 0 4px 16px rgba(212, 129, 111, 0.25);
		position: relative;
		overflow: hidden;
	}

	.btn-more-menu::before {
		content: '';
		position: absolute;
		top: 0;
		left: 0;
		right: 0;
		bottom: 0;
		background: linear-gradient(135deg, var(--color-primary-dark) 0%, var(--color-primary) 100%);
		opacity: 0;
		transition: opacity 0.3s;
	}

	.btn-more-menu:hover::before {
		opacity: 1;
	}

	.btn-more-menu:hover {
		transform: translateY(-2px);
		box-shadow: 0 8px 24px rgba(212, 129, 111, 0.35);
	}

	@media (max-width: 1024px) {
		.container {
			padding: 0 30px;
		}
	}

	@media (max-width: 768px) {
		.concerns {
			padding: 3rem 0;
		}

		.container {
			padding: 0 20px;
		}

		.section-title {
			font-size: 2rem;
		}

		.concerns-list {
			grid-template-columns: 1fr;
		}

		.symptoms-grid {
			grid-template-columns: repeat(3, 1fr);
			gap: 1rem;
		}

		.symptom-button {
			padding: 1.25rem 0.75rem;
		}

		.symptom-icon {
			font-size: 1.75rem;
		}

		.symptom-text {
			font-size: 0.875rem;
		}
	}
</style>
