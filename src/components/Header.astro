---
---

<header class="header">
	<div class="header-top">
		<div class="container">
			<div class="header-content">
				<a href="/" class="logo">
					<img src="/logo.png" alt="からだサポートSUZURAN" class="logo-image" />
					<h1>からだサポート<br />SUZURAN</h1>
				</a>
				<div class="header-info">
					<div class="contact-info">
						<div class="address">
							<span>📍 福島県いわき市内郷内町四方北114-3</span>
						</div>
						<div class="phone">
							<a href="tel:090-2999-3760" class="phone-link">
								<span class="phone-icon">📞</span>
								<span class="phone-number">090-2999-3760</span>
							</a>
						</div>
					</div>
					<a href="https://suzuran-women.com/" target="_blank" class="btn-line">ご予約はこちら</a>
				</div>
			</div>
		</div>
	</div>
	<nav class="nav">
		<div class="container">
			<ul class="nav-list">
				<li><a href="/">HOME</a></li>
				<li><a href="/first">初めての方</a></li>
				<li><a href="/menu">メニュー</a></li>
				<li><a href="/access">アクセス</a></li>
				<li><a href="/price">料金表</a></li>
			</ul>
		</div>
	</nav>
</header>

<style>
	.header {
		background: var(--color-bg-light);
		box-shadow: 0 2px 12px rgba(0, 0, 0, 0.06);
		position: sticky;
		top: 0;
		z-index: 100;
	}

	.container {
		max-width: 1200px;
		margin: 0 auto;
		padding: 0 20px;
	}

	.header-top {
		padding: 1rem 0;
	}

	.header-content {
		display: flex;
		justify-content: space-between;
		align-items: center;
		gap: 2rem;
	}

	.logo {
		display: flex;
		align-items: center;
		gap: 1rem;
		text-decoration: none;
	}

	.logo-image {
		width: 60px;
		height: 60px;
		object-fit: contain;
	}

	.logo h1 {
		margin: 0;
		font-size: 1.5rem;
		font-weight: 700;
		color: var(--color-primary);
		line-height: 1.3;
	}

	.header-info {
		display: flex;
		align-items: center;
		gap: 1.5rem;
	}

	.contact-info {
		display: flex;
		flex-direction: column;
		gap: 0.5rem;
	}

	.address {
		font-size: 0.9rem;
		color: var(--color-text-light);
	}

	.phone-link {
		display: flex;
		align-items: center;
		gap: 0.5rem;
		font-size: 1.25rem;
		font-weight: 700;
		color: var(--color-primary);
		transition: opacity 0.3s;
	}

	.phone-link:hover {
		opacity: 0.8;
	}

	.phone-icon {
		font-size: 1.5rem;
	}

	.btn-line {
		background: #06c755;
		color: white;
		padding: 0.75rem 2rem;
		border-radius: 50px;
		font-weight: 700;
		transition: transform 0.3s, box-shadow 0.3s;
		white-space: nowrap;
	}

	.btn-line:hover {
		transform: translateY(-2px);
		box-shadow: 0 4px 12px rgba(6, 199, 85, 0.3);
	}

	.nav {
		background: var(--color-bg-gray);
		border-top: 1px solid var(--color-border);
	}

	.nav-list {
		display: flex;
		list-style: none;
		margin: 0;
		padding: 0;
		justify-content: center;
		gap: 2rem;
	}

	.nav-list a {
		display: block;
		padding: 1rem 0.5rem;
		font-weight: 500;
		color: var(--color-text);
		transition: color 0.3s;
		border-bottom: 2px solid transparent;
	}

	.nav-list a:hover {
		color: var(--color-primary);
		border-bottom-color: var(--color-primary);
	}

	@media (max-width: 768px) {
		.header-top {
			padding: 0.5rem 0;
		}

		.container {
			padding: 0 15px;
		}

		.header-content {
			flex-direction: row;
			align-items: center;
			gap: 0.75rem;
		}

		.logo h1 {
			font-size: 1rem;
			line-height: 1.2;
		}

		.header-info {
			display: flex;
			flex-direction: row;
			align-items: center;
			gap: 0.5rem;
			flex: 1;
			justify-content: flex-end;
		}

		.contact-info {
			gap: 0.25rem;
		}

		.address {
			display: none;
		}

		.phone-link {
			font-size: 0.9rem;
			gap: 0.25rem;
		}

		.phone-icon {
			font-size: 1.1rem;
		}

		.btn-line {
			padding: 0.5rem 1rem;
			font-size: 0.85rem;
		}

		.nav-list {
			flex-wrap: nowrap;
			gap: 0;
			font-size: 0.8rem;
			overflow-x: auto;
			-webkit-overflow-scrolling: touch;
		}

		.nav-list li {
			flex: 0 0 auto;
			min-width: auto;
			text-align: center;
		}

		.nav-list a {
			padding: 0.5rem 0.75rem;
			font-size: 0.85rem;
			white-space: nowrap;
		}
	}
</style>
