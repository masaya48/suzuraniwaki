---
import { Image } from 'astro:assets';
import Icon from './Icon.astro';
import top1 from '../assets/top1.jpg';
import top2 from '../assets/top2.jpg';
import top3 from '../assets/top3.jpg';
import top4 from '../assets/top4.jpg';
import lineIcon from '../assets/LINE_Brand_icon.png';
---

<section class="hero" id="home">
	<div class="hero-bg-decoration"></div>
	<div class="hero-content">
		<div class="hero-text">
			<div class="hero-badge">福島県いわき市唯一の「重症症状専門」整体院</div>
			<h1 class="hero-title">
				<span class="hero-title-line">背骨と骨盤を整えて</span>
				<span class="hero-title-line hero-title-accent">根本から改善</span>
			</h1>
			<p class="hero-description">
				からだサポートSUZURANは、DRT整体（背骨調整法）を中心に、<br class="hide-mobile" />
				一人ひとりの体と心に寄り添う施術を提供しています。
			</p>
			<p class="hero-description">
				整形外科、接骨院どこに行っても良くならない症状にお困りの方へ<br class="hide-mobile" />
				<span class="hero-title-accent">医師に見放された重度の症状</span>でも、
				当院の施術で根本から改善します。
			</p>
			<ul class="hero-features">
				<li>
					<Icon name="sparkles" size={24} class="feature-icon" />
					<span class="feature-text">DRT整体（背骨調整法）</span>
				</li>
				<li>
					<Icon name="user-check" size={24} class="feature-icon" />
					<span class="feature-text">日本DRT協会インストラクター在籍</span>
				</li>
				<li>
					<Icon name="baby" size={24} class="feature-icon" />
					<span class="feature-text">産後骨盤矯正・マタニティ整体</span>
				</li>
				<li>
					<Icon name="car" size={24} class="feature-icon" />
					<span class="feature-text">駐車場あり（5台完備）</span>
				</li>
			</ul>
			<div class="reservation-info">完全予約制</div>
			<div class="hero-cta">
				<a href="tel:090-2999-3760" class="btn-primary">
					<Icon name="phone" size={24} class="btn-icon" />
					<span class="btn-text">
						<span class="btn-label">お電話でのご予約</span>
						<span class="btn-sublabel">090-2999-3760</span>
					</span>
				</a>
				<a href="line://ti/p/%40hss5518z" class="btn-secondary">
					<Image src={lineIcon} alt="LINE" width={24} height={24} class="line-icon" />
					<span class="btn-text-simple">LINE予約</span>
				</a>
			</div>
		</div>
		<div class="hero-visual">
			<div class="hero-image-frame">
				<div class="hero-image-placeholder">
					<div class="slideshow-container">
						<div class="slide active">
							<Image src={top1} alt="施術イメージ写真1" />
						</div>
						<div class="slide">
							<Image src={top2} alt="施術イメージ写真2" />
						</div>
						<div class="slide">
							<Image src={top3} alt="施術イメージ写真3" />
						</div>
						<div class="slide">
							<Image src={top4} alt="施術イメージ写真4" />
						</div>
							<!-- 追加の画像はここに追加できます -->
					</div>
					<div class="hero-decoration-circle"></div>
					<div class="hero-decoration-line"></div>
				</div>
			</div>
	</div>
</section>

<style>
	.hero {
		position: relative;
		min-height: 85vh;
		display: flex;
		align-items: center;
		padding: 6rem 0 4rem;
		overflow: hidden;
		background: linear-gradient(to bottom, var(--color-bg-light), var(--color-bg));
	}

	.hero-bg-decoration {
		position: absolute;
		top: -10%;
		right: -5%;
		width: 50%;
		height: 70%;
		background: radial-gradient(
			circle at center,
			var(--color-cream) 0%,
			transparent 70%
		);
		opacity: 0.6;
		border-radius: 50%;
		pointer-events: none;
		animation: float 20s ease-in-out infinite;
	}

	@keyframes float {
		0%, 100% { transform: translate(0, 0) scale(1); }
		50% { transform: translate(-20px, -30px) scale(1.05); }
	}

	.hero-content {
		position: relative;
		max-width: 1200px;
		margin: 0 auto;
		padding: 0 40px;
		display: grid;
		grid-template-columns: 1.1fr 0.9fr;
		gap: 5rem;
		align-items: center;
		animation: fadeIn 1s ease-out;
	}

	.hero-text {
		animation: fadeInUp 1s ease-out;
	}

	.hero-badge {
		display: inline-block;
		padding: 0.5rem 1.25rem;
		background: var(--color-cream);
		color: var(--color-primary-dark);
		border-radius: 50px;
		font-size: 0.875rem;
		font-weight: 600;
		letter-spacing: 0.05em;
		margin-bottom: 1.5rem;
		border: 1px solid var(--color-border);
		animation: fadeInUp 1s ease-out 0.2s backwards;
	}

	.hero-title {
		margin: 0 0 1.5rem 0;
		font-family: var(--font-display);
		font-size: 3.5rem;
		line-height: 1.2;
		letter-spacing: 0.01em;
		animation: fadeInUp 1s ease-out 0.3s backwards;
	}

	.hero-title-line {
		display: block;
		white-space: nowrap;
		background: linear-gradient(
			135deg,
			var(--color-text) 0%,
			var(--color-text-light) 100%
		);
		-webkit-background-clip: text;
		-webkit-text-fill-color: transparent;
		background-clip: text;
	}

	.hero-title-accent {
		background: linear-gradient(
			135deg,
			var(--color-primary) 0%,
			var(--color-accent) 100%
		);
		-webkit-background-clip: text;
		-webkit-text-fill-color: transparent;
		background-clip: text;
		position: relative;
	}

	.hero-title-accent::after {
		content: '';
		position: absolute;
		bottom: 0.2em;
		left: 0;
		width: 60%;
		height: 0.15em;
		background: linear-gradient(
			90deg,
			var(--color-accent) 0%,
			transparent 100%
		);
		border-radius: 10px;
		opacity: 0.4;
	}

	.hero-description {
		font-size: 1.05rem;
		line-height: 1.9;
		color: var(--color-text-light);
		margin: 0 0 2.5rem 0;
		animation: fadeInUp 1s ease-out 0.4s backwards;
	}

	.hero-features {
		list-style: none;
		padding: 0;
		margin: 0 0 3rem 0;
		display: grid;
		grid-template-columns: repeat(2, 1fr);
		gap: 1rem;
		animation: fadeInUp 1s ease-out 0.5s backwards;
	}

	.hero-features li {
		display: flex;
		align-items: center;
		gap: 0.75rem;
		padding: 0.75rem 1rem;
		background: white;
		border-radius: 12px;
		border: 1px solid var(--color-border);
		transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
	}

	.hero-features li:hover {
		transform: translateY(-3px);
		box-shadow: 0 8px 24px rgba(212, 129, 111, 0.12);
		border-color: var(--color-primary);
	}

	.feature-icon {
		flex-shrink: 0;
		color: var(--color-primary);
	}

	.feature-text {
		font-size: 0.95rem;
		font-weight: 500;
		color: var(--color-text);
	}

	.reservation-info {
		text-align: center;
		color: var(--color-accent);
		font-weight: 700;
		font-size: 1.1rem;
		animation: fadeInUp 1s ease-out 0.5s backwards;
	}

	.hero-cta {
		display: flex;
		gap: 1.25rem;
		flex-wrap: wrap;
		animation: fadeInUp 1s ease-out 0.6s backwards;
	}

	.btn-primary,
	.btn-secondary {
		display: inline-flex;
		align-items: center;
		gap: 0.75rem;
		padding: 1.25rem 2.25rem;
		border-radius: 60px;
		font-weight: 600;
		font-size: 1rem;
		transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
		text-align: center;
		white-space: nowrap;
		cursor: pointer;
		border: none;
	}

	.btn-primary {
		background: linear-gradient(135deg, var(--color-primary) 0%, var(--color-accent) 100%);
		color: white;
		box-shadow: 0 8px 24px rgba(212, 129, 111, 0.3);
		position: relative;
		overflow: hidden;
	}

	.btn-primary::before {
		content: '';
		position: absolute;
		top: 0;
		left: 0;
		right: 0;
		bottom: 0;
		background: linear-gradient(135deg, var(--color-primary-dark) 0%, var(--color-primary) 100%);
		opacity: 0;
		transition: opacity 0.4s;
	}

	.btn-primary:hover::before {
		opacity: 1;
	}

	.btn-primary:hover {
		transform: translateY(-3px);
		box-shadow: 0 12px 32px rgba(212, 129, 111, 0.4);
	}

	.btn-primary .btn-text,
	.btn-primary .btn-icon {
		position: relative;
		z-index: 1;
	}

	.btn-text {
		display: flex;
		flex-direction: column;
		align-items: flex-start;
		gap: 0.15rem;
	}

	.btn-label {
		font-size: 1rem;
		font-weight: 600;
	}

	.btn-sublabel {
		font-size: 0.875rem;
		font-weight: 500;
		opacity: 0.95;
	}

	.btn-secondary {
		background: linear-gradient(135deg, #06c755 0%, #00b900 100%);
		color: white;
		border: none;
		box-shadow: 0 4px 16px rgba(6, 199, 85, 0.25);
		position: relative;
		overflow: hidden;
	}

	.btn-secondary::before {
		content: '';
		position: absolute;
		top: 0;
		left: 0;
		right: 0;
		bottom: 0;
		background: linear-gradient(135deg, #00b900 0%, #009900 100%);
		opacity: 0;
		transition: opacity 0.3s;
	}

	.btn-secondary:hover::before {
		opacity: 1;
	}

	.btn-secondary:hover {
		transform: translateY(-3px);
		box-shadow: 0 8px 24px rgba(6, 199, 85, 0.35);
	}

	.btn-secondary .btn-text-simple,
	.btn-secondary .line-icon {
		position: relative;
		z-index: 1;
	}

	.line-icon {
		width: 24px;
		height: 24px;
		flex-shrink: 0;
	}

	.btn-icon {
		flex-shrink: 0;
	}

	/* Hero Visual */
	.hero-visual {
		position: relative;
		animation: fadeInUp 1s ease-out 0.4s backwards;
	}

	.hero-image-frame {
		position: relative;
		width: 100%;
	}

	.hero-image-placeholder {
		width: 100%;
		aspect-ratio: 3/4;
		background: linear-gradient(
			135deg,
			var(--color-cream) 0%,
			var(--color-cream-light) 100%
		);
		border-radius: 32px;
		display: flex;
		align-items: center;
		justify-content: center;
		color: var(--color-text-light);
		font-size: 1.1rem;
		font-weight: 500;
		box-shadow:
			0 20px 60px rgba(212, 129, 111, 0.2),
			0 0 0 1px var(--color-border);
		position: relative;
		overflow: hidden;
	}

	.hero-image-placeholder::before {
		content: '';
		position: absolute;
		top: 0;
		left: 0;
		right: 0;
		bottom: 0;
		background:
			radial-gradient(circle at 30% 40%, rgba(212, 129, 111, 0.1) 0%, transparent 50%),
			radial-gradient(circle at 70% 60%, rgba(95, 122, 97, 0.08) 0%, transparent 50%);
	}

	.slideshow-container {
		position: relative;
		width: 100%;
		height: 100%;
	}

	.slide {
		position: absolute;
		top: 0;
		left: 0;
		width: 100%;
		height: 100%;
		opacity: 0;
		transition: opacity 1s ease-in-out;
	}

	.slide.active {
		opacity: 1;
	}

	.slide img {
		width: 100%;
		height: 100%;
		object-fit: cover;
		display: block;
	}

	.hero-decoration-circle {
		position: absolute;
		top: -8%;
		right: -8%;
		width: 35%;
		aspect-ratio: 1;
		background: linear-gradient(
			135deg,
			var(--color-secondary-light) 0%,
			var(--color-secondary) 100%
		);
		border-radius: 50%;
		opacity: 0.15;
		z-index: -1;
		animation: float 15s ease-in-out infinite;
	}

	.hero-decoration-line {
		position: absolute;
		bottom: 10%;
		left: -12%;
		width: 25%;
		height: 4px;
		background: linear-gradient(
			90deg,
			var(--color-accent) 0%,
			transparent 100%
		);
		border-radius: 10px;
		opacity: 0.6;
	}

	.hide-mobile {
		display: inline;
	}

	@media (max-width: 1024px) {
		.hero-content {
			padding: 0 30px;
			gap: 3rem;
		}

		.hero-title {
			font-size: 2.75rem;
		}
	}

	@media (max-width: 768px) {
		.hero {
			padding: 3rem 0 2rem;
			min-height: auto;
		}

		.hero-content {
			grid-template-columns: 1fr;
			gap: 3rem;
			padding: 0 20px;
		}

		.hero-title {
			font-size: 2rem;
		}

		.hero-features {
			grid-template-columns: 1fr;
		}

		.hero-cta {
			flex-direction: column;
			width: 100%;
		}

		.btn-primary,
		.btn-secondary {
			width: 100%;
			justify-content: center;
		}

		.hide-mobile {
			display: none;
		}

		.hero-visual {
			order: -1;
		}

		.hero-image-placeholder {
			aspect-ratio: 4/3;
		}
	}

	@media (max-width: 480px) {
		.hero-title {
			font-size: 1.65rem;
		}
	}

	@media (max-width: 360px) {
		.hero-title {
			font-size: 1.4rem;
		}
	}
</style>

<script>
	document.addEventListener('DOMContentLoaded', () => {
		const slides = document.querySelectorAll('.slide');
		if (slides.length <= 1) return; // 1枚しかない場合はスライドショー不要

		let currentSlide = 0;

		function nextSlide() {
			slides[currentSlide].classList.remove('active');
			currentSlide = (currentSlide + 1) % slides.length;
			slides[currentSlide].classList.add('active');
		}

		// 3秒ごとに切り替え
		setInterval(nextSlide, 3000);
	});
</script>
