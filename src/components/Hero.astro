---
---

<section class="hero" id="home">
	<div class="hero-content">
		<div class="hero-text">
			<h2 class="hero-title">
				<span class="hero-subtitle">福島県いわき市の整体院</span>
				<span class="hero-main">背骨と骨盤を整えて<br />根本から改善</span>
			</h2>
			<ul class="hero-features">
				<li>✓ DRT整体（背骨調整法）</li>
				<li>✓ 日本DRT協会インストラクター在籍</li>
				<li>✓ 産後骨盤矯正・マタニティ整体</li>
				<li>✓ 駐車場あり（5台完備）</li>
			</ul>
			<div class="hero-cta">
				<a href="tel:090-2999-3760" class="btn-primary">
					<span class="btn-icon">📞</span>
					お電話でのご予約
				</a>
				<a href="https://suzuran-women.com/" target="_blank" class="btn-secondary">
					<span class="btn-icon">🌐</span>
					公式サイトはこちら
				</a>
			</div>
		</div>
		<div class="hero-image">
			<div class="hero-image-placeholder">
				<p>施術イメージ写真</p>
			</div>
		</div>
	</div>
</section>

<style>
	.hero {
		background: linear-gradient(135deg, #f0f9ff 0%, #e0f2fe 100%);
		padding: 4rem 0;
	}

	.hero-content {
		max-width: 1200px;
		margin: 0 auto;
		padding: 0 20px;
		display: grid;
		grid-template-columns: 1fr 1fr;
		gap: 3rem;
		align-items: center;
	}

	.hero-title {
		margin: 0 0 2rem 0;
	}

	.hero-subtitle {
		display: block;
		font-size: 1rem;
		font-weight: 500;
		color: var(--color-primary);
		margin-bottom: 0.5rem;
	}

	.hero-main {
		display: block;
		font-size: 2.5rem;
		font-weight: 700;
		line-height: 1.3;
		color: var(--color-text);
	}

	.hero-features {
		list-style: none;
		padding: 0;
		margin: 0 0 2rem 0;
	}

	.hero-features li {
		font-size: 1.1rem;
		margin-bottom: 0.75rem;
		color: var(--color-text);
		font-weight: 500;
	}

	.hero-cta {
		display: flex;
		gap: 1rem;
		flex-wrap: wrap;
	}

	.btn-primary,
	.btn-secondary {
		display: inline-flex;
		align-items: center;
		gap: 0.5rem;
		padding: 1rem 2rem;
		border-radius: 50px;
		font-weight: 700;
		font-size: 1rem;
		transition: all 0.3s;
		text-align: center;
		white-space: nowrap;
	}

	.btn-primary {
		background: var(--color-primary);
		color: white;
		box-shadow: 0 4px 12px rgba(37, 99, 235, 0.3);
	}

	.btn-primary:hover {
		background: var(--color-secondary);
		transform: translateY(-2px);
		box-shadow: 0 6px 16px rgba(37, 99, 235, 0.4);
	}

	.btn-secondary {
		background: #06c755;
		color: white;
		box-shadow: 0 4px 12px rgba(6, 199, 85, 0.3);
	}

	.btn-secondary:hover {
		background: #05b04d;
		transform: translateY(-2px);
		box-shadow: 0 6px 16px rgba(6, 199, 85, 0.4);
	}

	.btn-icon {
		font-size: 1.25rem;
	}

	.hero-image-placeholder {
		width: 100%;
		aspect-ratio: 4/3;
		background: linear-gradient(135deg, #e5e7eb 0%, #d1d5db 100%);
		border-radius: 12px;
		display: flex;
		align-items: center;
		justify-content: center;
		color: var(--color-text-light);
		font-size: 1.1rem;
		font-weight: 500;
	}

	@media (max-width: 768px) {
		.hero {
			padding: 2rem 0;
		}

		.hero-content {
			grid-template-columns: 1fr;
			gap: 2rem;
		}

		.hero-main {
			font-size: 1.75rem;
		}

		.hero-cta {
			flex-direction: column;
		}

		.btn-primary,
		.btn-secondary {
			width: 100%;
			justify-content: center;
		}
	}
</style>
